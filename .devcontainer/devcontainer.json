{
    "name": "aoc2015",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "remoteUser": "elf",
    "extensions": [
        "ms-python.python"
    ],
    "postCreateCommand": "pip install --user -r requirements.txt; echo export PATH=\"$HOME/.local/bin:$PATH\" >> $HOME/.bashrc"
}
